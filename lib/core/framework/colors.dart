import 'package:flutter/material.dart';

//
// Clase para almacenar todos los colores necesario para la app como constantes estáticas.
//
class ColorsTheme {
  static const primary = Color(0xFF131313);
  static const secondary = Color(0xffea4c89);
  static const background = primary;
  static const buttonBackground = Color(0xFFFCF4EE);

  static const fontColorPrimary = Color(0xFF717171);
  static const white = Color(0xFFFFFFFF);
  static const backgroundSecondary = Color(0xff302c2c);

  //
  // Colores de los estados de las películas.
  //
  static const rumored = Color(0xFF957F01);
  static const planned = Color(0xFF007258);
  static const inProduction = Color(0xFF0054F0);
  static const postProduction = Color(0xFF053600);
  static const released = Color(0xFFB30790);
  static const canceled = Color(0xFFF01900);
}
